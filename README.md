# nasa-cmapss-predictive-maintenance
AI-based predictive maintenance project that estimates the Remaining Useful Life (RUL) of turbofan engines using the NASA C-MAPSS dataset. Implements a Random Forest regression model trained on multivariate sensor data with evaluation metrics such as RMSE, MAE, and R².
